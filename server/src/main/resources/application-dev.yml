spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/book_library
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: dev
    show-sql: true
  liquibase:
    enabled: false
app:
  cors:
    allowed-origins: "http://localhost:3000"


